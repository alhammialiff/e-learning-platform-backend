import { SectionMultimedia } from "./SectionMultimedia";

export interface Section {
    sectionNumber: number,
    sectionTitle: string,
    sectionDescription: string,
    sectionOutcome: string,
    sectionMultimedia: SectionMultimedia
}
  